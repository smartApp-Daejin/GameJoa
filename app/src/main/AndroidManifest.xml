<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daejin.gamejoa">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameJoa">
        <activity
            android:name=".board.BoardEditActivity"
            android:exported="true" />
        <activity
            android:name=".setting.SettingActivity"
            android:exported="true" />
        <activity android:name=".board.BoardInsideActivity" />
        <activity android:name=".board.BoardWriteActivity" />
        <activity android:name=".contensList.ContentShowActivity" />
        <activity android:name=".contensList.ContentsListActivity" />
        <activity android:name=".auth.JoinActivity" />
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".auth.IntroActivity" />
        <activity android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>